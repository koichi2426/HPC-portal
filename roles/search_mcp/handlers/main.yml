---
- name: Web検索MCPを再起動
  ansible.builtin.systemd:
    name: hpc-search-mcp
    state: restarted
    daemon_reload: true
  when:
    - not ansible_check_mode
    - not (hpc_force_service_restarts | default(false) | bool)
